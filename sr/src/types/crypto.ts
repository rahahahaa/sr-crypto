export interface CryptoData {
  symbol: string;
  price: string;
  volume: string;
  change24h: string;
  marketCap: string;
}

export interface CryptoResponse {
  symbol: string;
  price: string;
  volume: string;
  priceChangePercent: string;
}